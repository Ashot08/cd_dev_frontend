export const UsersFilter = (props) => {

    return (<>

        <form>

            <input type="date" placeholder={'от'}/>
            <input type="date" placeholder={'до'}/>


        </form>

    </>)
}